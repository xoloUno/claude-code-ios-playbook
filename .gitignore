# Claude Code (ignore everything except rules/, commands/, and templates/)
.claude/*
!.claude/rules/
!.claude/commands/
!.claude/templates/

# Local credentials (never commit)
.env.playbook
.env.project

# macOS
.DS_Store
._*
